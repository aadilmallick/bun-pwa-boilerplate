{
  "name": "pwa-tutorial",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun build --outdir=./dist ./frontend/index.ts ./backend/sw.ts && cd dist && bunx vite ."
  }
}
