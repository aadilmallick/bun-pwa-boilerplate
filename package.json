{
  "name": "pwa-tutorial",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun build --outdir=./app ./frontend/index.ts ./sw.ts && cd app && bunx vite ."
  }
}
